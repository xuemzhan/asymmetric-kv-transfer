# Experiment data artifacts (regenerable)
# reports/*.json is deliberately NOT ignored: the JSON reports are the
# provenance record for every number in the paper (they are required before a
# number enters main.tex, and the cluster/statistics scripts read them back).
data/*.json

# Python
__pycache__/
*.pyc

# Temp
/tmp/

# Agent session dumps / notebooks
.omo/
.ipynb_checkpoints/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/main.pdf
paper/*.bbl
paper/*.blg
paper/main.bcf
paper/main.run.xml

# Run artifacts (regenerable)
runs/

# Downloaded Tectonic binary (not source)
tools/tectonic/

# Figure rasters (regenerable from the tracked vector PDFs)
paper/figures/*.png

# arXiv bundle: source and figures are tracked, the compiled PDF is not
paper/arxiv_submission/main.pdf
